/*4. Accept one character from user and convert case of that character.
Input: a
Output : A
Input : D
Output: d
*/

#include<stdio.h>
void Display Convert (   CValue)
{
        if( )
        {
           printf("%c",)
        }
           else if( )
        {
            printf("% );
        }
}

int main()
{
    char cValue = '\0';
    printf("Enter character\n");
    scanf("%c",&cValue);
         (cValue);
return 0;
}
